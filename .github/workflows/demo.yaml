name: Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello!"
      - name: Checkout repo code
        uses: actions/checkout@v6
      - name: list files in repo
        run: |
          ls ${{ github.workspace  }}
      - run: echo "this job statuds is ${{ job.status }}"
# de
